/**
 * Demo dataset definitions used by the public seeder (`seedDemoData`).
 * Each entry produces one experiment_preset and one example completed run.
 *
 * Names are prefixed with "[Demo]" so users can identify and delete them.
 */

export type DemoPreset = {
  name: string;
  case_name: string;
  prompt: string;
  recommendation: string;
  baselineViolations: number;
  postViolations: number;
  feasibility: "feasible" | "infeasible" | "unknown";
  notes: string;
};

export const DEMO_PRESETS: DemoPreset[] = [
  {
    name: "[Demo] Light load relief — case5",
    case_name: "case5",
    prompt:
      "Network case5 shows two overloaded lines. Recommend a single corrective action that reduces total violations while keeping the system feasible.",
    recommendation:
      "Apply a global load scaling of 0.9 (scale_all_loads value=0.9). This relieves both overloaded branches without curtailing generation, preserves voltage feasibility, and is the minimal-impact intervention.",
    baselineViolations: 2,
    postViolations: 0,
    feasibility: "feasible",
    notes: "Demo run — generated by seedDemoData. Deterministic DC-solver evaluation.",
  },
  {
    name: "[Demo] Generator redispatch — case14",
    case_name: "case14",
    prompt:
      "Case14 has one overloaded transmission line during peak hours. Suggest a generator redispatch that resolves the violation.",
    recommendation:
      "Reduce generator at index 1 to 25 MW (set_generator_p_mw target_index=1 value=25). Slack absorbs the difference; line loading drops below 90% with no new violations.",
    baselineViolations: 1,
    postViolations: 0,
    feasibility: "feasible",
    notes: "Demo run — generated by seedDemoData. Deterministic DC-solver evaluation.",
  },
  {
    name: "[Demo] Contingency analysis — case30",
    case_name: "case30",
    prompt:
      "On case30, an N-1 outage of line 5 produces cascading overloads. Identify the most effective single corrective action.",
    recommendation:
      "Open line 12 (line_outage target_index=12). Re-routing flow through the parallel corridor clears the post-contingency overload; remaining margin is acceptable.",
    baselineViolations: 3,
    postViolations: 1,
    feasibility: "feasible",
    notes: "Demo run — generated by seedDemoData. Deterministic DC-solver evaluation.",
  },
];
